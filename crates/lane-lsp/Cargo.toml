[package]
name = "lane-lsp"
version = "0.1.0"
edition = "2021"

[dependencies]
lane = { path = "../.." }
tokio = { version = "1", features = ["io-std", "macros", "rt-multi-thread"] }
tower-lsp = "0.20"
tree-sitter = "0.26.8"
tree-sitter-language = "0.1.7"
